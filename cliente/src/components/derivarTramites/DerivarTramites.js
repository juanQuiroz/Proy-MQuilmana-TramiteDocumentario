import React, { Fragment } from "react";
import Navbar from "../layouts/Navbar";

const DerivarTramites = () => {
  return (
    <Fragment>
      <Navbar />
      Deben aparecer todos los tramites que le corresponden a esta gerencia y
      que hayan sido aceptados para su posterior derivacion
    </Fragment>
  );
};

export default DerivarTramites;
